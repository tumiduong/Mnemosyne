import React from "react";
import './PracticeTerm.css';

export default function PracticeTerm(props) {

  return (

    <div>
      <div></div>
      <div>
        <p>{props.term}</p>
      </div>

    </div>

  );
}